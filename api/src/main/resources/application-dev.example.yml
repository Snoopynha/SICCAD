spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/seubanco
    username: postgres
    password: suasenha
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true